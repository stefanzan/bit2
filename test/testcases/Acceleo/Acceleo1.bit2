«var c ={name:"Person", fields:[{name:"name", type:"String"},{name:"tel", type:"String"}], operations:[]}»
public class «c.name» {
  public «c.name»(«for a in c.fields separator ","»«a.type» «a.name»«endfor») {
  «for a in c.fields»
    this.«a.name»=«a.name»;
  «endfor»
  }
  «for a in c.fields»
  final private «a.type» «a.name»;
  public «a.type» get«a.name»() {
    return this.«a.name»;
  }
  «endfor»
  «for o in c.operations»
  public «o.retType» «o.name»() {
    // TODO should be implemented
  }
  «endfor»
}